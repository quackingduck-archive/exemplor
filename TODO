* exemplor executable that can run multiple example files
* #teardown
* Use fancy unicode characters for tests status:
* TextMate bundle that does nice cmd+r output
# (i) = ∙
# (I) = •
# (s) = ✓
# (f) = ✗
# (e) = ☠
* Change non-tty output to:
  - name: some test name
    status: OK (no checks)
    result: output